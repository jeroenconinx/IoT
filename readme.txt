start: Wi-Fi hotspot with SSID: "vrijdagtest" and password: "12345678"
server pc should connect to this wifi

1) navigate to /mosquitto and run start.bat in terminal
2) navigate to localhost:8088 with username:"cedalo" password:"mmcisawesome"
3) navigate to in a terminal to /webserver and run:"npm start" to start nodejs backend link between frontend and mqtt broker
4) open /frontend/index.html to view front end
5) run psoc